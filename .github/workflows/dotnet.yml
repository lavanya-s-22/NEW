name: .NET
on:
   push:
     branches: ["TRIAL"]
   pull_request:
      branches: ["TRIAL"]
jobs:
  build:
    runs-on: windows-latest
    steps: 
    - uses: actions/checkout@v3
    - name: setup .NET
      uses: action/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x
        
        
      
      
    
